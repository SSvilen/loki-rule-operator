---
kind: pipeline
type: docker
name: arm64 Image

platform:
  os: linux
  arch: amd64

steps:
  - name: build
    image: plugins/docker
    settings:
      repo: foo/bar
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
---

